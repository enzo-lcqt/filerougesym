<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="{{ asset('CSS/style.css') }}">
        {% block stylesheets %}
		</title>

		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
		<link
		rel="stylesheet" href="{{ asset('CSS/style.css') }}">

		{# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
	
			{# {{ encore_entry_link_tags('app') }} #}
		{% endblock %}
			<script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
			<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
		{% block javascripts %}
			{# {{ encore_entry_script_tags('app') }} #}
		{% endblock %}
	</head>


    {# le header #}
<nav class="cc-navbar navbar navbar-expand-lg navbar-dark position-fixed w-100">
    <div class="container-fluid">
        <a class="navbar-brand text-uppercase mx-4 py-3 fw-bolder" style='color:white;' href="{{ path('app_accueil') }}">The District</a>
        <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent"
            aria-expanded="false"
            aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item pe-4">
                    <a class="nav-link active " aria-current="page" href="{{ path('app_accueil') }}">Accueil</a>
                </li>
                <li class="nav-item pe-4">
                    <a class="nav-link active" href="{{ path('app_categories') }}">Découvrir</a>
                </li>
                <li class="nav-item pe-4">
                    <a class="nav-link active" href="{{ path('app_plats') }}">Plats</a>
                </li>
                 <li class="nav-item pe-4">
                    <a class="nav-link active" href="{{ path('app_contact') }}">Contact</a>
                </li> 
                {# <li class="nav-item pe-4">
                    <a class="btn btn-order rounded-0 active" href="{{ path('app_commande') }}" style="color: #fff">Passer une commande</a>
                </li>
                <li class="nav-item pe-4">
                    <a class="nav-link active" href="{{ path('app_logout') }}">Déconnexion</a>
                </li> #}
            </ul>
        </div>
    </div>
</nav>
<body>
</body>

		{% block body %}
        {% endblock %}
        
</html>